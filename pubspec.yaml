name: pinterest
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.2

dependencies:
  image_picker:
  cupertino_icons:
  get: 
  permission_handler: 
  shared_preferences:
  google_nav_bar:
  flutter_cache_manager:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
